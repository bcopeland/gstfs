EXTRA_DIST=Makefile README COPYING mp32wav.sh ogg2mp3.sh

LDADD = @gstfs_LIBS@

# don't require INSTALL, NEWS, AUTHORS, ChangeLog
AUTOMAKE_OPTIONS=foreign

bin_PROGRAMS=gstfs
gstfs_SOURCES=gstfs.c xcode.c xcode.h
